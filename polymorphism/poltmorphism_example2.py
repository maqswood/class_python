x = 'python'
print(len(x))

a = ['python', 'html', 'django', 'css']
print(len(a))
# These two examples are using same method
# But we get different result
# one is the letter of string and next is the element in list
